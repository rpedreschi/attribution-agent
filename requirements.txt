# Runtime dependencies for the DeltaStream Attribution Agent (Phase 0 demo)

# Mock data generator -> Kafka (Confluent Cloud)
confluent-kafka>=2.3,<3.0

# Agent runtime: Claude via direct Anthropic API (local CLI) or AWS Bedrock
anthropic>=0.40,<1.0
boto3>=1.34,<2.0

# Spreadsheet artifact
openpyxl>=3.1,<4.0

# Config + validation
PyYAML>=6.0,<7.0
pydantic>=2.6,<3.0
