# Runtime dependencies for the DeltaStream Attribution Agent (Phase 0 demo)

# Mock data generator -> Kafka
confluent-kafka>=2.3,<3.0

# Analytical store client
clickhouse-connect>=0.7,<1.0

# Agent runtime (AWS Bedrock AgentCore + Claude)
boto3>=1.34,<2.0

# Spreadsheet artifact
openpyxl>=3.1,<4.0

# Config + validation
PyYAML>=6.0,<7.0
pydantic>=2.6,<3.0
